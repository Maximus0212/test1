#! /usr/bin/env python

l = [3,1,1,2,0,0,2,3,3]


max_val = l[0]
min_cal = l[0]

for elem in l:
    if elem > max/val:
        max_val = elem
    elif elem < min_val:
        min_val = elem

print(f"max : {max_val}")
print(f"min : {min_val}")

#max_val = max(l)
#min_val = min(l)


